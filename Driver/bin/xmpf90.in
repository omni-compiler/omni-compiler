#!/bin/sh

# $TSUKUBA_Release: Omni OpenMP Compiler 3 $
# $TSUKUBA_Copyright:
#  PLEASE DESCRIBE LICENSE AGREEMENT HERE
#  $

BINDIR=`dirname $0`

i=0
for arg in "$@"; do
    case "$arg" in
#        -fopenmp)
#            args[$i]="--Wf-fopenmp"
#            for arg2 in "@OMF_CONF_LNK_OPT@"; do
#                i=$(expr $i + 1)
#               args[$i]="--Wn$arg2"
#            done ;;
        -tmp)
            args[$i]="--Wn$arg";;
        *)
            args[$i]="$arg";;
    esac
    i=$(expr $i + 1)
done

set -- "${args[@]}"

${BINDIR}/omdriver --xf -Txmpf "$@"

exit $?

